#let neq = $eq.not$
#let geq = $>=$
#let leq = $<=$
#let iff = $<==>$
#let qed = align(right, $square.filled$)
#let pf = text(style: "italic", [proof:])
#show regex("proof"): it => [_Proof:_]



Binomial theorem trick
Triangle inequality Trck


Convergence:

For every $epsilon > 0$, there
is an integer $N$, such that
$d(x_n, p) < epsilon$
whenever $n geq N$
